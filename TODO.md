# Enhancements
[x] Different queue behaviors
    [x] "Small chat mode" - queues messages as they come in unless the queue is full
    [x] "Big chat mode" - queues one message, waits until it's finished speaking, then queues the newest message (helps keep speech going without a crazy backlogged queue)
[ ] Onboarding process
    [ ] Check if browser provides voices, recommend using Edge (most voices) or Chrome if not
    [ ] Project description
    [ ] Pick between small chat/big chat/import from a backup

# Future Features (post 1.0 release)
[ ] Toggleable chat commands
[ ] Sound effects
[ ] Word replacements